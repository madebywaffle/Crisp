--- a/io/papermc/paper/entity/activation/ActivationRange.java
+++ b/io/papermc/paper/entity/activation/ActivationRange.java
@@ -166,6 +_,28 @@
                 }
 
                 ActivationRange.activateEntity(entity);
+
+                // Crisp start - DAB
+                if (io.github.madebywaffle.crisp.CrispConfig.dabEnabled && entity.getType().dabEnabled
+                        && (!io.github.madebywaffle.crisp.CrispConfig.dabDontEnableIfInWater
+                            || !entity.isInWater()
+                            || entity.is(net.minecraft.tags.EntityTypeTags.CAN_BREATHE_UNDER_WATER))
+                        && !isThrottleExempt(entity)) {
+                    if (!entity.activatedPriorityReset) {
+                        entity.activatedPriorityReset = true;
+                        entity.activatedPriority = io.github.madebywaffle.crisp.CrispConfig.maximumActivationPrio;
+                    }
+                    net.minecraft.world.phys.Vec3 playerVec = player.position();
+                    net.minecraft.world.phys.Vec3 entityVec = entity.position();
+                    double diffX = playerVec.x - entityVec.x, diffY = playerVec.y - entityVec.y, diffZ = playerVec.z - entityVec.z;
+                    int squaredDistance = (int) (diffX * diffX + diffY * diffY + diffZ * diffZ);
+                    entity.activatedPriority = squaredDistance > io.github.madebywaffle.crisp.CrispConfig.startDistanceSquared ?
+                            Math.max(1, Math.min(squaredDistance >> io.github.madebywaffle.crisp.CrispConfig.activationDistanceMod, entity.activatedPriority)) :
+                            1;
+                } else {
+                    entity.activatedPriority = 1;
+                }
+                // Crisp end - DAB
             }
         }
     }
@@ -175,6 +_,20 @@
      *
      * @param entity
      */
+    // Crisp start - DAB: keep full-rate AI during moments where throttling is player-visible
+    private static boolean isThrottleExempt(final Entity entity) {
+        if (entity instanceof net.minecraft.world.entity.Mob mob) {
+            if (mob.getTarget() != null || mob.getLastHurtByMob() != null) {
+                return true; // fighting or recently hurt
+            }
+            if (mob instanceof net.minecraft.world.entity.animal.Animal animal && animal.isInLove()) {
+                return true; // breeding
+            }
+        }
+        return false;
+    }
+    // Crisp end - DAB
+
     private static void activateEntity(final Entity entity) {
         if (MinecraftServer.currentTick > entity.activatedTick) {
             if (entity.defaultActivationState) {
